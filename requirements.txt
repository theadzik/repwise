# Runtime and development dependencies.
# The package itself is installable: pip install -e .
#
# Garmin Connect API client. Versions >=0.3.5 rebuilt the login on curl_cffi,
# which is what keeps it working after Garmin's March 2026 auth change.
garminconnect>=0.3.6

# workouts.yaml parsing
pyyaml

# Tests
pytest
